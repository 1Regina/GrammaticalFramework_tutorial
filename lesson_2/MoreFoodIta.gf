concrete MoreFoodIta of MoreFood = FoodIta ** open Prelude in {
    lincat
     Question = SS ; -- bcos MoreFood.gf cat = Question. Then see StringOper for Question is SS
    lin
     QIs item quality = ss (item.s ++ "è" ++ quality.s) ;
     Pizza = ss "pizza" ;
}