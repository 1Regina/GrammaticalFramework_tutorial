concrete MoreFoodChi of MoreFood = FoodChi ** open StringOper in {
    lincat
     Question = SS ; -- bcos MoreFood.gf cat = Question. Then see StringOper for Question is SS
    lin
     QIs item quality = ss (item.s ++ "是不是" ++ quality.s) ;
     Pizza = ss "比萨" ;
}